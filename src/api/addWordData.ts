export const addWordData = async (
  clientId: string,
  fromLanguage: string,
  toLanguage: string,
  word: string,
  meaning: string,
  pronunciation: string
): Promise<void> => {
  // api call
};

export const mockAddWordData = async (
    clientId: string,
    fromLanguage: string,
    toLanguage: string,
    word: string,
    meaning: string,
    pronunciation: string
  ): Promise<void> => {
    // api call
  };
